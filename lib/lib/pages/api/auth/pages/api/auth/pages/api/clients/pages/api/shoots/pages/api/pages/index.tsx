veis:</h2>
      <ul>
              <li><strong>POST</strong> /api/auth/register — Cadastrar usuário</li>
                      <li><strong>POST</strong> /api/auth/login — Fazer login</li>
                              <li><strong>GET/POST/DELETE</strong> /api/clients — Clientes (requer token)</li>
                                      <li><strong>GET/POST/DELETE</strong> /api/shoots — Ensaios (requer token)</li>
                                              <li><strong>GET/POST</strong> /api/sync — Sincronizar todos os dados (requer token)</li>
                                                    </ul>
                                                          <p style={{color:'#666',fontSize:'0.9rem'}}>Use o header: Authorization: Bearer TOKEN</p>
                                                              </div>
                                                                );
                                                                }